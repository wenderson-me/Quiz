<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#2979FF</item>
        <item name="colorPrimaryDark">#2979FF</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Button Cancel Theme -->
    <style name="ChangeButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:textColor">@color/text</item>
        <item name="android:background">@drawable/click_button</item>
        <item name="android:textAlignment">textStart</item>
    </style>

</resources>
